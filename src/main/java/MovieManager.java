public class MovieManager {
    private String movieName;
    public movieManager(String movieName) {
        this.movieName = movieName;
    }
    private MovieManager[] movie = new MovieManager[0];

    get set
}
